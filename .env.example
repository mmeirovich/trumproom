# Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# OpenAI API Configuration
# Required: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Specify OpenAI model (defaults to gpt-3.5-turbo if not set)
# MODEL=gpt-4

# Serper is a google search API that is used for the initial research.
# It has a nice free tier.
SERPER_API_KEY=your_serper_api_key_here


# Setup Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Edit the .env file with your actual values
# 3. Never commit the .env file to version control (it's in .gitignore)
# 4. Get your OpenAI API key from: https://platform.openai.com/api-keys
